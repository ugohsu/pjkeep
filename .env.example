# Flask セッション署名用キー（必ず変更してください）
# 生成例: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-a-random-secret-key

# コンテナが作成するファイルのオーナーをホストユーザーに合わせる
# 確認方法: id -u  /  id -g
APP_UID=1000
APP_GID=1000
